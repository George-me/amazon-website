/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "m.media-amazon.com",
      "images-eu.ssl-images-amazon.com",
      "www.amazon.ae",
    ],
  },
};

export default nextConfig;
